[tool.poetry]
name = "data_extractor"
version = "0.4.0.dev3"
license = "MIT"
description = "Combine XPath, CSS Selector and JSONPath for Web data extracting."
authors = ["linw1995 <linw1995@icloud.com>"]
readme = "README.md"
homepage = "https://github.com/linw1995/data_extractor"
repository = "https://github.com/linw1995/data_extractor"
documentation = "https://linw1995.com/data_extractor"
classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.7",
    "Development Status :: 4 - Beta",
    "Operating System :: POSIX",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
]
include = ["CHANGES.md", "HISTORY.md", "LICENSE"]

[tool.poetry.dependencies]
python = "^3.7"
cssselect = "^1.0.3"
jsonpath-rw = "^1.4.0"
lxml = "^4.3.0"
jsonpath-rw-ext = "^1.2"

[tool.poetry.dev-dependencies]
rope = {version = "^0.14.0", extras = ["linting"], optional = true}
black = {version = "^19.3b0", extras = ["linting"], optional = true}
flake8 = {version = "^3.7.8", extras = ["linting"], optional = true}
isort = {version = "^4.3.21", extras = ["linting"], optional = true}
pytest = {version = "^5.2.0", extras = ["test"], optional = true}
pytest-cov = {version = "^2.7.1", extras = ["test"], optional = true}
mypy = {version = "^0.730", extras = ["linting"], optional = true}
sphinx = {version = "^2.2", extras = ["docs"], optional = true}

[tool.poetry.extras]
linting = ["rope", "black", "flake8", "isort"]
test = ["pytest", "pytest-cov"]
docs = ["sphinx"]
